---
phase: 03-word-bank-and-srs-foundation
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/utils/grammarEngine.js
  - src/utils/grammarEngine.test.js
  - src/data/story.js
autonomous: true

must_haves:
  truths:
    - "Grammar engine nouns are derived from the unified word bank (no separate hardcoded noun list)"
    - "Grammar engine sentences still produce correct Hebrew gender agreement after noun sync"
    - "ENCOURAGEMENT messages use gender-neutral phrasing or provide boy/girl variants"
    - "NPC dialogues that address the player use gender-appropriate forms"
  artifacts:
    - path: "src/utils/grammarEngine.js"
      provides: "Grammar engine with nouns derived from word bank, adjectives/verbs kept separate"
      contains: "import.*initialWordData.*from.*words"
    - path: "src/utils/grammarEngine.test.js"
      provides: "Updated tests reflecting noun list from word bank"
      min_lines: 50
    - path: "src/data/story.js"
      provides: "Gender-audited ENCOURAGEMENT messages and NPC dialogues"
      contains: "boy.*girl|correct_boy.*correct_girl"
  key_links:
    - from: "src/utils/grammarEngine.js"
      to: "src/data/words.js"
      via: "import initialWordData for noun extraction"
      pattern: "import.*initialWordData.*from.*words"
    - from: "src/utils/grammarEngine.js"
      to: "VOCAB.nouns"
      via: "buildNounsFromWordBank function"
      pattern: "buildNounsFromWordBank|filter.*category"
---

<objective>
Synchronize the grammar engine's noun vocabulary with the unified word bank and audit all ENCOURAGEMENT messages and NPC dialogues for correct Hebrew gender usage.

Purpose: CONT-08 requires grammar engine to use the unified word bank as its data source. CONT-09 requires all player-facing messages to be gender-appropriate.
Output: Updated `grammarEngine.js` consuming nouns from word bank, gender-audited `story.js`.
</objective>

<execution_context>
@/Users/galsened/.claude/get-shit-done/workflows/execute-plan.md
@/Users/galsened/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-word-bank-and-srs-foundation/03-RESEARCH.md
@.planning/phases/03-word-bank-and-srs-foundation/03-01-SUMMARY.md
@src/utils/grammarEngine.js
@src/utils/grammarEngine.test.js
@src/data/story.js
@src/data/words.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync grammar engine nouns from word bank and update tests</name>
  <files>src/utils/grammarEngine.js, src/utils/grammarEngine.test.js</files>
  <action>
**In `grammarEngine.js`:**

1. Import `initialWordData` from `'../data/words'`

2. Create a `buildNounsFromWordBank()` function that:
   - Filters `initialWordData` for words that are suitable as grammar sentence subjects
   - Suitable words: nouns from categories like 'animals', 'family', 'professions' (things that can be subjects of sentences like "The X is big" or "The X runs")
   - Maps each word to the grammar engine's noun format: `{ en: word.word, he: word.hebrew, gender: word.gender, emoji: word.emoji }`
   - Returns the array of noun objects

3. Replace the hardcoded `VOCAB.nouns` array with the result of `buildNounsFromWordBank()`. Keep the `VOCAB` object structure but make `nouns` dynamic:
   ```javascript
   const VOCAB = {
     nouns: buildNounsFromWordBank(),
     adjectives: [...],  // Keep hardcoded -- adjectives have he_m/he_f forms not in word schema
     verbs_transitive: [...],  // Keep hardcoded -- verbs have he_m/he_f forms
     verbs_intransitive: [...],  // Keep hardcoded
     objects: [...],  // Keep hardcoded
   };
   ```

4. **Do NOT change adjectives, verbs, or objects** -- these have gender-agreement forms (he_m/he_f) that are not in the word bank schema. Per research recommendation, only nouns sync from the word bank.

5. Ensure the existing `TEMPLATES` and `generateChallenge()` function require NO changes -- they already consume `VOCAB.nouns` generically.

**In `grammarEngine.test.js`:**

The existing tests mock `Math.random` to select specific nouns by index (e.g., index 0 = CAT, index 5 = PRINCESS). After this change, the noun list will come from the word bank and the indices will differ.

Update the tests to be resilient to noun list changes:

**Option A (preferred):** Mock `Math.random` to select the FIRST noun and verify it produces a valid sentence shape (starts with "THE", contains "IS"/"SLEEPS"/"LOVES", etc.) and that Hebrew output uses the correct gender form for that noun. The test can read `VOCAB.nouns[0]` to know what to expect.

**Option B:** Export the `VOCAB` constant (or `buildNounsFromWordBank`) and use it in tests to compute expected values dynamically.

The key invariant to test: **gender agreement is correct** -- masculine nouns get he_m adjectives/verbs, feminine nouns get he_f forms. This invariant doesn't depend on which specific nouns are in the list.

Ensure the test file still has at least 6 meaningful tests covering all 3 templates with both masculine and feminine nouns.
  </action>
  <verify>
`npx vitest run src/utils/grammarEngine.test.js` -- all tests pass. Grammar engine produces sentences using nouns from the word bank with correct gender agreement.
  </verify>
  <done>
Grammar engine nouns come from the unified word bank. Adjectives and verbs remain hardcoded (different schema). All grammar tests pass with correct gender agreement. No separate VOCAB noun list duplicates the word bank data.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix gender usage in ENCOURAGEMENT messages and NPC dialogues</name>
  <files>src/data/story.js</files>
  <action>
Audit every player-facing string in `story.js` for Hebrew gender correctness. The story already has boy/girl variants for `intro` and `completion` texts, but ENCOURAGEMENT and NPC dialogues are ungendered.

**ENCOURAGEMENT (lines ~413-452):**

Prefer gender-neutral rewrites where possible (remove "אתה/את", use root-form phrasing). Only use boy/girl objects for messages that genuinely cannot be made neutral. This minimizes code changes in consumers.

Specific messages to fix:

1. `correct` array (line ~414-423): `'אתה כוכב! ⭐'` is masculine -- rewrite to `'!כוכב ⭐'` (drop the pronoun).

2. `wrong` array (line ~425-431): `'אתה יכול! 💫'` is masculine -- rewrite to `'!אפשר 💫'` or similar neutral form. `'כמעט! נסה שוב 💪'` uses masculine imperative "נסה" -- rewrite to `'!כמעט! שוב 💪'` or convert to `{ boy: 'כמעט! נסה שוב 💪', girl: 'כמעט! נסי שוב 💪' }`.

3. `streak` messages (line ~433-439): `'אתה מכונה! 🤖'` -> `'!מכונה 🤖'`. The streak[20] message `'אתה החכם הצעיר ביותר!'` from CHAPTERS Hall of Sages NPC -> convert to `{ boy: '...', girl: '...' }` if it cannot be neutralized.

4. `lowLives` (line ~441-443): `'התרכז!'` is masculine imperative -> rewrite to `'!ריכוז ❤️'` or convert to `{ boy: 'התרכז! ❤️', girl: 'התרכזי! ❤️' }`.

5. `dailyReturn` (line ~446-451): `'בוקר טוב, גיבור! ☀️'` -> `{ boy: 'בוקר טוב, גיבור! ☀️', girl: 'בוקר טוב, גיבורה! ☀️' }`.

**NPC dialogues — actual structure and wiring:**

Each chapter has `npc.dialogues: [{ trigger: 'start'|'correct'|'wrong'|'streak_N'|'complete', text: '...' }]`.

The current `getNPCDialogue(level, trigger, playerName)` (line 514-522) finds the dialogue by trigger and does `dialogue.text.replace('{name}', playerName)`.

**Change the `text` field to a `{ boy, girl }` object** for dialogues that address the player with gendered Hebrew. Keep `text` as a plain string for gender-neutral dialogues. This matches the existing `intro`/`completion` pattern used throughout CHAPTERS.

Specific NPC dialogues to fix (all 5 chapters):

**easy (Queen Elizabeth):**
- `correct`: `'נפלא! אתה לומד מהר מאוד!'` -> `{ boy: 'נפלא! אתה לומד מהר מאוד!', girl: 'נפלא! את לומדת מהר מאוד!' }`
- `wrong`: `'לא נורא, נסה שוב. אני מאמינה בך!'` -> `{ boy: 'לא נורא, נסה שוב. אני מאמינה בך!', girl: 'לא נורא, נסי שוב. אני מאמינה בך!' }` (note: "מאמינה" is correct — the QUEEN is speaking, she's always feminine)
- `streak_3`: `'3 ברצף! אתה כוכב!'` -> `{ boy: '3 ברצף! אתה כוכב!', girl: '3 ברצף! את כוכבת!' }`

**medium (Fairy Luna):**
- `start`: `'שלום {name}! בוא נעוף בין העצים!'` -> `{ boy: 'שלום {name}! בוא נעוף בין העצים!', girl: 'שלום {name}! בואי נעוף בין העצים!' }`
- `wrong`: `'אופס! בוא ננסה שוב יחד.'` -> `{ boy: 'אופס! בוא ננסה שוב יחד.', girl: 'אופס! בואי ננסה שוב יחד.' }`
- `streak_5`: `'5 ברצף! אתה קוסם של מילים!'` -> `{ boy: '5 ברצף! אתה קוסם של מילים!', girl: '5 ברצף! את קוסמת של מילים!' }`

**hard (Merlin):**
- `correct`: `'חוכמה רבה יש בך, צעיר!'` -> `{ boy: 'חוכמה רבה יש בך, צעיר!', girl: 'חוכמה רבה יש בך, צעירה!' }`
- `wrong`: `'הקסם דורש סבלנות. נסה שוב.'` -> `{ boy: 'הקסם דורש סבלנות. נסה שוב.', girl: 'הקסם דורש סבלנות. נסי שוב.' }`

**expert (Alien Zorak):**
- `wrong`: `'גם בכוכב שלנו טועים. נסה שוב!'` -> `{ boy: '...נסה שוב!', girl: '...נסי שוב!' }`
- `streak_5`: `'וואו! 5 נכונות! אתה גאון!'` -> `{ boy: 'וואו! 5 נכונות! אתה גאון!', girl: 'וואו! 5 נכונות! את גאונית!' }`

**master (Great Sage):**
- `streak_5`: `'אתה החכם הצעיר ביותר!'` -> `{ boy: 'אתה החכם הצעיר ביותר!', girl: 'את החכמה הצעירה ביותר!' }`

Dialogues that are already gender-neutral (e.g., `'מוחך חד כמו לייזר!'`, `'החוכמה זורמת בך!'`, `'הכנפיים שלי מנצנצות משמחה!'`) remain as plain `text` strings.

**Update `getNPCDialogue()` (line 514-522):**

Add a `gender` parameter. Change the function signature and logic:

```javascript
export const getNPCDialogue = (level, trigger, playerName, gender = 'boy') => {
    const chapter = CHAPTERS[level];
    if (!chapter || !chapter.npc) return null;
    const dialogue = chapter.npc.dialogues.find(d => d.trigger === trigger);
    if (!dialogue) return null;
    // Support both plain text and { boy, girl } objects
    const text = typeof dialogue.text === 'string'
        ? dialogue.text
        : dialogue.text[gender] || dialogue.text.boy;
    return text.replace('{name}', playerName);
};
```

This is backward-compatible — callers not passing `gender` default to 'boy' (existing behavior). Callers that now pass gender get the correct variant.
  </action>
  <verify>
1. `npx vitest run` -- all tests pass
2. `grep -n "אתה " src/data/story.js` -- each occurrence is inside a `boy:` property, never standalone
3. `grep -n "נסה " src/data/story.js` -- each occurrence is inside a `boy:` property, never standalone
4. `getNPCDialogue('easy', 'correct', 'Test', 'girl')` returns feminine form
  </verify>
  <done>
All ENCOURAGEMENT messages are either gender-neutral or provide boy/girl variants. All NPC dialogues that address the player use { boy, girl } objects. `getNPCDialogue()` accepts `gender` parameter (defaults to 'boy' for backward compatibility) and resolves gendered dialogue objects. No masculine-only player-addressing text remains in story.js.
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run` -- full test suite passes (grammar engine tests + all others)
2. Grammar engine `generateChallenge()` produces sentences with nouns from the word bank
3. No hardcoded noun list in `grammarEngine.js` (VOCAB.nouns derived from import)
4. Gender audit: `grep -n "אתה " src/data/story.js` shows only boy-variant contexts, not standalone masculine text
5. NPC dialogues support gender parameter
</verification>

<success_criteria>
- Grammar engine nouns sourced from `initialWordData` (CONT-08)
- Adjectives/verbs remain grammar-engine-specific (different schema shape)
- All ENCOURAGEMENT messages gender-appropriate (CONT-09)
- All NPC dialogues gender-appropriate (CONT-09)
- Gender agreement in generated sentences still correct
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-word-bank-and-srs-foundation/03-04-SUMMARY.md`
</output>
