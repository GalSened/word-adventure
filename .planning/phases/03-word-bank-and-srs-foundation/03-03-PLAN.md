---
phase: 03-word-bank-and-srs-foundation
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/utils/srs.js
  - src/utils/srs.test.js
  - src/hooks/useGameLogic.js
autonomous: true

must_haves:
  truths:
    - "Review mode never surfaces unseen words — only previously-played words appear in review sessions"
    - "Review sessions contain at most 10 words (3 new low-rep + 7 overdue), preventing overwhelming sessions"
    - "Repeated reviews of the same word do not cluster on the same day — spacing varies naturally"
    - "The most overdue words appear first in review sessions so the player practices what they need most"
  artifacts:
    - path: "src/utils/srs.js"
      provides: "getDueWords (learned-only), addJitter, buildReviewSession"
      exports: ["calculateNextReview", "getDueWords", "addJitter", "buildReviewSession"]
    - path: "src/utils/srs.test.js"
      provides: "Updated tests for learned-only filtering, jitter, session caps, priority sorting"
      min_lines: 100
    - path: "src/hooks/useGameLogic.js"
      provides: "startLevel('review') using buildReviewSession"
      contains: "buildReviewSession"
  key_links:
    - from: "src/hooks/useGameLogic.js"
      to: "src/utils/srs.js"
      via: "import buildReviewSession"
      pattern: "import.*buildReviewSession.*from.*srs"
    - from: "src/utils/srs.js"
      to: "Math.random"
      via: "addJitter function"
      pattern: "Math\\.random\\(\\)"
---

<objective>
Fix the SRS algorithm to correctly distinguish learned from unseen words, add jitter to prevent review clustering, implement session caps, and sort by priority.

Purpose: The current `getDueWords()` treats unseen words as "due immediately" which floods review sessions. These fixes ensure the SRS works correctly at 200-word scale.
Output: Updated `srs.js` with tested functions, updated `useGameLogic.js` review flow.
</objective>

<execution_context>
@/Users/galsened/.claude/get-shit-done/workflows/execute-plan.md
@/Users/galsened/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-word-bank-and-srs-foundation/03-RESEARCH.md
@src/utils/srs.js
@src/utils/srs.test.js
@src/hooks/useGameLogic.js
</context>

<task type="tdd">
  <name>Task 1: RED — Write failing tests for SRS learned-only filtering, jitter, and session builder</name>
  <files>src/utils/srs.test.js</files>
  <action>
Write failing tests for all four SRS fixes before changing any production code.

**Expected behaviors to test (input -> output):**

**PROG-01 - getDueWords learned vs unseen:**
- `getDueWords([{word:'new'}])` -> `[]` (no SRS state = unseen = NOT due)
- `getDueWords([{word:'past', srs:{nextReviewDate: past}}])` -> `[{word:'past',...}]` (has SRS + overdue = due)
- `getDueWords([{word:'future', srs:{nextReviewDate: future}}])` -> `[]` (has SRS but not yet due)
- Update existing "returns words with no srs property" test to expect EMPTY array (PROG-01 reversal). Add explicit test: "excludes unseen words (no SRS state)".

**PROG-03 - addJitter:**
- `addJitter(1)` -> `1` (no jitter for 1-day intervals)
- `addJitter(10)` -> value between 9 and 11 (10 +/- 10%)
- `addJitter(100)` -> value between 90 and 110 (100 +/- 10%)
- Result is always >= 1 and always an integer
- Mock `Math.random` to test boundary values (0.0 -> -10%, 0.5 -> 0%, 1.0 -> +10%)

**PROG-02 + PROG-04 - buildReviewSession:**
- Returns empty array when no words have SRS state
- Returns max 7 overdue words from learned pool
- Returns overdue words sorted by nextReviewDate ascending (most overdue first)
- Never includes unseen words (no progress entry)
- Caps total at 10 words (7 review + 3 low-rep)
- Returns words with low repetition count as "new" slots (max 3)

**Jitter in calculateNextReview:**
- Existing nextReviewDate tests need updating to account for jitter. Either mock `Math.random` for deterministic tests or use range assertions (toBeGreaterThanOrEqual / toBeLessThanOrEqual).
  </action>
  <verify>
Run `npx vitest run src/utils/srs.test.js` — new/updated tests MUST FAIL (red). Existing unchanged tests may still pass. Commit: `test(03-03): add failing tests for SRS learned-only, jitter, session builder`
  </verify>
  <done>
Test file contains failing tests for all four PROG fixes. Running the tests produces expected failures for getDueWords unseen exclusion, addJitter, and buildReviewSession.
  </done>
</task>

<task type="tdd">
  <name>Task 2: GREEN + REFACTOR — Implement SRS fixes and wire buildReviewSession into useGameLogic</name>
  <files>src/utils/srs.js, src/hooks/useGameLogic.js</files>
  <action>
**GREEN — Make all failing tests pass:**

1. In `srs.js`:
   - Change `getDueWords`: `if (!word.srs) return false;` (was `return true;`)
   - Add `addJitter(intervalDays)`: no jitter for <=1, else +/-10% via `Math.random`, result rounded and >= 1
   - Add `buildReviewSession(allWords, userProgress, maxNew=3, maxReview=7)`:
     - Filter to words with progress entry (learned only)
     - Attach SRS state, filter to due (nextReviewDate <= now)
     - Sort by nextReviewDate ascending (PROG-04)
     - Slice overdue to maxReview (7)
     - Fill remaining slots with learned low-rep words not yet due (maxNew=3)
     - Return combined array (max 10)
   - Modify `calculateNextReview`: apply `addJitter(interval)` when computing nextReviewDate
   - Base `interval` field stays un-jittered (for algorithm continuity), `nextReviewDate` uses jittered interval
   - Export: `calculateNextReview`, `getDueWords`, `addJitter`, `buildReviewSession`

2. In `useGameLogic.js`:
   - Replace the `startLevel('review')` branch: use `buildReviewSession(initialWordData, store.userProgress)` instead of `getDueWords(allWordsWithState).slice(0, 10)`
   - Import `buildReviewSession` from `srs.js` (replace `getDueWords` import if no longer used directly)

**REFACTOR — Clean up:**
- Remove dead code from old getDueWords usage in useGameLogic
- Ensure jitter mock pattern is clean (vi.spyOn(Math, 'random'))
  </action>
  <verify>
Run `npx vitest run src/utils/srs.test.js` — ALL tests must pass (green). Then `npx vitest run` — full suite passes (no regressions). Commit GREEN: `feat(03-03): implement SRS fixes`, then REFACTOR if changes made: `refactor(03-03): clean up SRS implementation`
  </verify>
  <done>
All SRS tests pass. getDueWords excludes unseen words. addJitter applies +/-10% randomization. buildReviewSession enforces 3+7=10 cap with priority sorting. useGameLogic review mode uses buildReviewSession. Full test suite passes.
  </done>
</task>

<verification>
1. `npx vitest run src/utils/srs.test.js` -- all SRS tests pass (old tests updated, new tests green)
2. `npx vitest run` -- full suite passes (no regressions in other test files)
3. Unseen words never appear in review session (test proves this)
4. Session caps enforced (test proves max 7 review + 3 new = 10)
5. Jitter produces values within expected range (deterministic Math.random mock tests)
</verification>

<success_criteria>
- `getDueWords` returns only words WITH SRS state that are overdue (PROG-01)
- `buildReviewSession` caps at 3 new + 7 review, never includes unseen (PROG-02)
- `addJitter` applies +/-10% randomization to intervals > 1 day (PROG-03)
- Due words sorted most-overdue-first (PROG-04)
- All existing and new SRS tests pass
- `useGameLogic.startLevel('review')` uses buildReviewSession
</success_criteria>

<output>
After completion, create `.planning/phases/03-word-bank-and-srs-foundation/03-03-SUMMARY.md`
</output>
