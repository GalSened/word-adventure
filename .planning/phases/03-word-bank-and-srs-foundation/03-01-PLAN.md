---
phase: 03-word-bank-and-srs-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/wordSchema.js
  - src/data/words.js
autonomous: true

must_haves:
  truths:
    - "The word bank contains 200+ entries validated by Zod with zero validation errors"
    - "Words span 10 themed categories with at least 15 words per category"
    - "Every word has correct Hebrew grammatical gender (m/f/n) and bilingual example sentences"
    - "Gender-aware hints use hint_m/hint_f only where Hebrew grammar requires it (player-addressing phrases)"
  artifacts:
    - path: "src/data/wordSchema.js"
      provides: "Extended Zod schema with hint_m, hint_f, exampleSentence_he, category enum"
      contains: "z.enum.*animals.*food.*family"
    - path: "src/data/words.js"
      provides: "200+ validated word entries across 10 categories"
      min_lines: 500
  key_links:
    - from: "src/data/words.js"
      to: "src/data/wordSchema.js"
      via: "validateWords import at module load"
      pattern: "validateWords\\(rawWordData\\)"
---

<objective>
Extend the Zod word schema with gender-aware hint fields, bilingual example sentences, and a category enum, then author 200+ words across 10 themed categories with correct Hebrew metadata.

Purpose: Establishes the content foundation that all other Phase 3 plans build on -- audio, SRS at scale, and grammar engine sync all require a large, validated word bank.
Output: Extended `wordSchema.js` and expanded `words.js` with 200+ entries passing fail-fast validation.
</objective>

<execution_context>
@/Users/galsened/.claude/get-shit-done/workflows/execute-plan.md
@/Users/galsened/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-word-bank-and-srs-foundation/03-RESEARCH.md
@src/data/wordSchema.js
@src/data/words.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Zod word schema with gender hints, bilingual examples, and category enum</name>
  <files>src/data/wordSchema.js</files>
  <action>
Modify `WordSchema` in `src/data/wordSchema.js` to add these fields:

1. **`hint_m`** — `z.string().optional()` — Masculine hint variant (when Hebrew grammar requires addressing the player as male)
2. **`hint_f`** — `z.string().optional()` — Feminine hint variant (when Hebrew grammar requires addressing the player as female)
3. **`exampleSentence_he`** — `z.string().min(1)` — Hebrew translation of the example sentence (REQUIRED for every word)
4. **`category`** — Change from `z.string().min(1)` to `z.enum(['animals', 'food', 'family', 'colors', 'nature', 'body', 'actions', 'home', 'emotions', 'professions'])` — Locked enum of 10 categories per CONT-02

Keep all 10 existing required fields exactly as they are (per Phase 2 decision [02-04]). The 3 new fields are: `hint_m` (optional), `hint_f` (optional), `exampleSentence_he` (required).

Do NOT use `.extend()` -- just modify the existing `z.object()` call directly since this is the only schema definition.

Export the `WORD_CATEGORIES` array (the 10 category values) as a named export for use by other modules.
  </action>
  <verify>
Run `npx vitest run` -- existing tests should still pass since the 13 current words will get `exampleSentence_he` added in Task 2. If tests fail due to word validation at import time, that confirms the schema extension is enforcing the new required field correctly (expected -- will be resolved in Task 2).
  </verify>
  <done>
`WordSchema` validates all 13 fields (10 original + hint_m optional + hint_f optional + exampleSentence_he required). Category is a 10-value enum. `WORD_CATEGORIES` is exported.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author ~100 words for categories: animals, food, family, colors, nature</name>
  <files>src/data/words.js</files>
  <action>
Update the `rawWordData` array in `src/data/words.js`. First, update the existing 13 words: change their `category` values to match the new enum (e.g., `'objects'` -> `'home'` for book/treasure) and add `exampleSentence_he` to each. Then add ~100 NEW word entries for the first 5 categories.

**Category targets for this task (minimum 15 per category, ~100 total):**

| Category | Examples | Target Count |
|----------|----------|--------------|
| animals | cat, dog, fish, bird, elephant, lion, monkey, horse, rabbit, turtle, bear, snake, frog, whale, butterfly | 20+ |
| food | apple, bread, milk, pizza, rice, egg, cake, banana, cheese, juice, soup, chicken, salad, cookie, water | 20+ |
| family | mother, father, sister, brother, baby, grandmother, grandfather, uncle, aunt, cousin, son, daughter, friend, child, parent | 20+ |
| colors | red, blue, green, yellow, orange, purple, pink, white, black, brown, gray, gold, silver | 15+ |
| nature | sun, moon, tree, flower, rain, cloud, star, mountain, river, sea, wind, fire, snow, sky, earth | 20+ |

**Level distribution (based on English word complexity):**
- `easy`: 3-4 letter words (cat, dog, sun, red, run, egg)
- `medium`: 5-6 letter words (happy, water, mouth, brave, table)
- `hard`: 7-8 letter words (elephant, chicken, brother, teacher)
- `expert`: 9+ letter words (butterfly, firefighter, grandmother, extraordinary)

**For each word entry, provide ALL required fields:**
```javascript
{
  id: 'cat',                          // Lowercase English word (unique)
  word: 'CAT',                        // UPPERCASE English
  hebrew: 'חתול',                     // Hebrew translation
  hint: '🐱 חיה שאוהבת חלב',          // Hebrew hint (gender-neutral about the word)
  // hint_m and hint_f ONLY when hint addresses the player directly
  category: 'animals',                // One of 10 enum values
  emoji: '🐱',                        // Representative emoji
  level: 'easy',                      // Based on English word length
  type: 'word',                       // Always 'word' for vocabulary entries
  gender: 'm',                        // Hebrew grammatical gender (m/f, 'n' only for truly neuter concepts)
  exampleSentence: 'The cat sleeps on the sofa.',     // English example
  exampleSentence_he: 'החתול ישן על הספה.',           // Hebrew translation of example
}
```

**Gender-aware hints (CONT-04):** Most words need only the gender-neutral `hint` field (describing the object/concept). Add `hint_m`/`hint_f` ONLY for the ~10-15% of words where the hint directly addresses the player using gendered Hebrew (e.g., "אתה/את", gendered verbs like "נסה/נסי", gendered adjectives about the player).

**Quality rules:**
- Every Hebrew translation must be correct for the target meaning
- Gender must match Hebrew grammar (e.g., שמש is feminine, ספר is masculine)
- Example sentences should be simple, age-appropriate (ages 6-10)
- Hints should help a Hebrew-speaking child understand the English word
- IDs must be unique (lowercase English word, no duplicates)

Keep the `validateWords(rawWordData)` call at the bottom -- it will catch any schema violations at module load time.
  </action>
  <verify>
Run `npx vitest run` -- all tests must pass, confirming words validate against the extended schema. Verify word count: `grep -c "id:" src/data/words.js` should show ~113 (13 existing + ~100 new).
  </verify>
  <done>
`words.js` contains ~113 word entries across 5 categories (animals, food, family, colors, nature) plus the 13 updated originals. Each has correct Hebrew translation, gender, hint, bilingual example sentences, and difficulty level. Module loads without validation errors. All existing tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author ~100 words for categories: body, actions, home, emotions, professions</name>
  <files>src/data/words.js</files>
  <action>
Continue adding word entries to the `rawWordData` array in `src/data/words.js` for the remaining 5 categories. Append after the entries from Task 2.

**Category targets for this task (minimum 15 per category, ~100 total):**

| Category | Examples | Target Count |
|----------|----------|--------------|
| body | head, hand, eye, heart, mouth, nose, ear, leg, foot, arm, finger, tooth, hair, face, back | 20+ |
| actions | run, jump, eat, sleep, read, write, sing, dance, swim, fly, cook, draw, play, walk, climb | 20+ |
| home | table, chair, door, window, bed, lamp, clock, phone, pencil, paper, cup, plate, key, mirror, stairs | 20+ |
| emotions | happy, sad, brave, scared, angry, tired, excited, proud, shy, kind, strong, clever, funny, calm, curious | 20+ |
| professions | teacher, doctor, firefighter, police, pilot, chef, artist, farmer, driver, nurse, singer, soldier, builder, baker, writer | 20+ |

**Same field requirements as Task 2.** All fields required per schema.

**Gender-aware hints (CONT-04):** The emotions and actions categories are most likely to need `hint_m`/`hint_f` variants because their hints may address the player directly (e.g., "!נסה לדמיין שאתה מרגיש כך" / "!נסי לדמיין שאת מרגישה כך"). Add these only where Hebrew grammar requires it.

**Quality rules (same as Task 2):**
- Every Hebrew translation must be correct for the target meaning
- Gender must match Hebrew grammar
- Example sentences should be simple, age-appropriate (ages 6-10)
- Hints should help a Hebrew-speaking child understand the English word
- IDs must be unique (no duplicates with Task 2 words)
  </action>
  <verify>
Run `npx vitest run` -- all tests must pass. Verify total word count: `grep -c "id:" src/data/words.js` should show 200+. Verify all 10 categories have at least 15 words: `grep -o "category: '[^']*'" src/data/words.js | sort | uniq -c` should show 10 categories each with 15+.
  </verify>
  <done>
`words.js` contains 200+ word entries across all 10 categories. Each has correct Hebrew translation, gender, hint, bilingual example sentences, and difficulty level. Module loads without validation errors. All existing tests pass. No duplicate IDs.
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run` -- all tests pass (no regressions from schema extension or word data changes)
2. App loads without errors (fail-fast validation succeeds at module import)
3. Word count: `grep -c "id:" src/data/words.js` returns 200+
4. Category coverage: `grep -o "category: '[^']*'" src/data/words.js | sort | uniq -c` shows all 10 categories with at least 15 words
5. No duplicate IDs: `grep "id:" src/data/words.js | sort | uniq -d` returns empty
</verification>

<success_criteria>
- WordSchema enforces 13 fields (10 original required + hint_m/hint_f optional + exampleSentence_he required) with category as a 10-value enum
- 200+ words pass Zod validation at module load time
- Every word has correct Hebrew gender, natural Hebrew hint, and bilingual example sentence
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-word-bank-and-srs-foundation/03-01-SUMMARY.md`
</output>
